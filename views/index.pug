html
  head
    title Test
    link(rel='stylesheet', href='index.css')
  body
    h1 #{title || "Test body"}
    // a is declared in the server-side code
    - var b = "test(client HTML)";
    - var c = a + (message !== false ? "" + b : "");
    b #{c}
    script(src="http://api.songle.jp/v2/api.js")
    - if (message !== false) {
    script(src="index.js")
    - }
